bin
obj
appsettings.json
local.settings.json
.vscode

# Azure Functions artifacts
.python_packages
__pycache__
*.user

# NOTE: Az modules under Modules/ MUST be committed/deployed with the app
# (managed dependencies are not available on Linux Consumption), so they are
# intentionally NOT ignored here. Run ./install-modules.ps1 to populate them.
